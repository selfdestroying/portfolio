---
interface Props {
	size?: number
	color?: string
}
const { size = 16, color = 'currentColor', ...rest } = Astro.props
---

<svg
	xmlns='http://www.w3.org/2000/svg'
	width={size}
	height={size}
	fill={color}
	{...rest}
	viewBox='0 0 256 256'
>
	<slot />
</svg>
